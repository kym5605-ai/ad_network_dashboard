# 제휴 캠페인 기여도 대시보드 기획안 (v0.5 · 2026-09-21)

> 원천: `aff_data.js`(캠페인별 일별 총계 + 지면별 노출·클릭) · 인벤토리 payload `IV`(지면별 네트워크 eCPM)
>
> **v0.4 (2026-09-21 사용자 지시) — 지면을 두 갈래로 나눈다**
> ① **네트워크 공유 지면 = 커뮤니티 보드·커뮤니티 네이티브 2개뿐** → 여기서만 **네트워크 vs 제휴** 판정이 필요하고, 캠페인 간(디즈니 vs 애플) 비교도 필요
> ② **비공유 지면**(하단팝업·데일리메시지·홈카드·홈배너·홈아이콘 등) → **캠페인 간 비교만** 되면 된다. 네트워크 비교 불필요
> ③ **「스팟 전부 끄는 날」 확보는 어려울 수 있다** → 그 전제 없이 굴러가는 설계로 교체(§3-4)
>
> **v0.5 (2026-09-21) — 두 가지 정정**
> ⑴ **기준선을 `eCPM` → `eCPM × 게재율`(요청당 실효수익)로 교정.** 제휴 물량을 네트워크로 돌려도 게재율만큼만 팔린다
> ⑵ 🔴 **기간을 묶으면 결론이 뒤집힌다.** 2026 전체(Q1 업사이즈 6% 포함)로 보면 애플이 제휴 우위로 나오지만,
>    **최근 90일 BAU로 보면 애플은 네트워크 우위**다. 판단은 **최근 90일 기준**으로 한다
> ⑶ 원화 밴드(10배 폭)를 폐기하고 **손익분기 문턱값**으로 대체 — 밴드는 추정 구간이 아니라 「가정의 범위」였다
>
> v0.3 유지: 정가 CPM 미적용 · 스플래시 제외 · 화면 이름·배치 보류

---

## 1. 지표

```
R-eCPM (노출당 매출 기여액) = 제휴 매출 ÷ 제휴 노출 × 1,000
```

단가표는 쓰지 않는다. 분자는 실제 들어온 돈, 분모는 adsystem 실측 노출.

| | 분자 | 분모 |
|---|---|---|
| 애플 임팩트 | Impact 액션수익 × 환율(1,437.8) | adsystem 지면별 노출 합 |
| 디즈니 플러스 | 구독 건수 × 10,000원 | adsystem 지면별 노출 합 |

**분모에서 제외**: 스플래시(클릭 구조 없음) · proxy 지면(`홈 혜택`·`혜택 페이지`·`앱 푸시` — 값이 이벤트 수) · 미집행일

> 스플래시 제외 효과: 애플 최근 30일 51원 → **77원(+51%)**, 디즈니 307원 → **386원(+26%)**

---

## 2. 축이 셋이다 — 무엇을 어디서 비교하는가

| 축 | 비교 대상 | 필요한 지면 | 배분 필요? | 상태 |
|---|---|---|---|---|
| **A. 캠페인 축** | 디즈니 vs 애플 (전체) | 전부 합산 | **불필요** | ✅ **지금 가능 · 전부 실측** |
| **B. 비공유 지면 축** | 같은 지면에서 디즈니 vs 애플 | 하단팝업·데일리메시지·홈카드·홈배너·홈아이콘 등 | 필요하나 **왜곡이 상쇄됨** | ✅ **지금 가능 · 지수로만** |
| **C. 공유 지면 축** | 제휴 vs 네트워크 + 캠페인 간 | **커뮤니티 보드·네이티브 2개** | 필요 · **왜곡이 치명적** | 🔴 **현재 판정 불가**(§3-3) |

**A 실측치** (스플래시 제외)

| 캠페인 | 6~8월 | 최근 30일 |
|---|---:|---:|
| 디즈니 플러스 | 232원 | **386원** |
| 애플 임팩트 | 85원 | **77원** |
| 격차 | 2.7배 | **5.0배** |

---

## 3. 🔴 지면별 전환을 모른다 — 축마다 다루는 법이 다르다

```
노출당 매출 = CTR      ×  클릭당 전환   ×  전환당 매출
              (지면별 실측)  (지면별 미지)    (캠페인 실측)
```

미지수는 **지면별 클릭당 전환(CVR)** 하나다.

### 3-1. ⛔ 클릭 비례 배분의 성질

클릭 비례는 **모든 지면 CVR이 같다**는 가정이다. 넣으면 식이 이렇게 된다:

```
지면별 R-eCPM = (전체 클릭당 매출) × CTR_지면 × 1,000
```

→ **지면별 R-eCPM 순위 = CTR 순위.** 새 정보를 0 생산한다. 실제 결과:

| 캠페인 | 하단팝업 CTR | 클릭 점유 | 클릭비례 시 스팟 지면이 가져가는 매출 |
|---|---:|---:|---:|
| 애플 임팩트 | 11.70% | 84.1% | **93.0%** |
| 디즈니 플러스 | 11.28% | 78.0% | **85.6%** |

하단팝업은 **액션 1건에 38,778클릭**(데일리메시지 39·홈아이콘 45의 **880배 비효율** — 팝업 닫으려다 눌린 클릭).

### 3-2. ✅ B축(비공유 지면)에서는 이 왜곡이 **상쇄된다** — 지금 바로 쓸 수 있다

B축이 답해야 할 질문은 **「하단팝업 구좌가 비었는데 애플에 줄까 디즈니에 줄까」** 다.
절대 원화가 아니라 **같은 지면에서 캠페인끼리의 상대 비교**면 된다.

클릭 비례의 왜곡은 **지면 고유**다(하단팝업은 두 캠페인 모두 나쁘다). 같은 지면에서 캠페인끼리 비교하면 **왜곡 계수가 양쪽에 비슷하게 걸려 상당 부분 약분된다.**

**→ B축은 원화가 아니라 「지수」로 표시한다.**

```
지면 기여지수 = 그 캠페인의 지면별 노출당 기여 ÷ 그 캠페인 전체 평균
```

- ✅ 같은 지면에서 **캠페인 간 비교 가능**
- ⛔ **절대 원화 표기 금지 · 네트워크 비교 금지 · 지면끼리 순위 매기기 금지**
- 전제: 지면별 CVR 왜곡의 **방향과 크기가 두 캠페인에서 비슷하다**. 각주로 고정 표기.

### 3-3. ✅ C축 해법 — 원화를 추정하지 말고 **손익분기**를 계산한다

원화 밴드는 배분 방법에 따라 10배씩 갈린다. 그건 추정 구간이 아니라 **가정의 범위**라 비교에 못 쓴다.
대신 **귀속 없이 정확히 계산되는 값**으로 질문을 바꾼다.

```
손익분기 매출점유 = 네트워크 요청당 실효수익 ÷ 캠페인 평균 R-eCPM
                 = 「이 지면이 캠페인 평균의 몇 %만 벌면 본전인가」
```

분자·분모 모두 실측이다. **배분이 전혀 필요 없다.**

**기준선 (요청당 실효수익 = 네트워크 eCPM × 게재율)**

| 지면 | 기간 | 네트워크 eCPM | 게재율 | **기준선** |
|---|---|---:|---:|---:|
| 커뮤니티 보드 | 최근 90일 | 180원 | 56.1% | **101원** |
| 커뮤니티 네이티브 | 최근 90일 | 131원 | 71.4% | **93원** |

> ⚠️ eCPM을 그대로 기준선으로 쓰면 보드는 네트워크 가치를 **1.8배 과대평가**한다.
> 기존 프로젝트 원칙 「판단은 게재율 × eCPM(요청당 실효수익)」과 정합한다.

**판정 — 최근 90일 (BAU) ← 이게 지금 판단의 기준**

| 칸 | 캠페인 평균 R-eCPM | **손익분기** | 클릭 기준 추정(하한) | 판정 |
|---|---:|---:|---:|---|
| 네이티브 × 디즈니 | 283원 | **33%** | 29% | ⚠️ **보류 — 거의 붙어 있다** |
| 보드 × 디즈니 | 283원 | **36%** | 16% | ⚠️ 보류 |
| 네이티브 × 애플 | 83원 | **112%** | 25% | ✕ **네트워크 우위** |
| 보드 × 애플 | 83원 | **121%** | 15% | ✕ **네트워크 우위** |

- **애플은 답이 났다.** 손익분기가 112~121% — 보드·네이티브가 **애플 캠페인 평균보다 더 잘 벌어야** 본전인데,
  둘 다 CTR이 가장 낮은 축의 지면이다. 사실상 불가능하다.
- **디즈니는 평균의 1/3만 내도 본전**이다(33~36%). 클릭 기준 추정은 16~29%로 못 미치지만
  **클릭 기준은 저CTR 지면을 과소평가하는 하한**이라 실제는 위일 수 있다. 네이티브는 29% vs 33%로 거의 붙어 있다.
- 갈리는 이유는 단순하다 — **디즈니 평균 R-eCPM이 283원으로 애플(83원)의 3.4배**라 같은 기준선에 대해 문턱이 훨씬 낮다.

**🔴 기간을 묶으면 결론이 뒤집힌다**

| 칸 | 최근 90일 (BAU) | 2026 전체 (Q1 업사이즈 포함) |
|---|---|---|
| 보드 × 애플 | 손익분기 **121%** → 네트워크 우위 | 손익분기 **10%** → 제휴 우위 |
| 네이티브 × 애플 | 손익분기 **112%** → 네트워크 우위 | 손익분기 **13%** → 제휴 우위 |

애플의 2026 전체 평균 R-eCPM이 861원인 건 **Q1 업사이즈(수수료 6%)가 끌어올린 값**이다. 최근 BAU는 83원이다.
→ **애플은 절대로 기간을 묶어서 보면 안 된다.** 화면 기본값은 최근 90일로 고정한다.

### 3-4. 회귀(MMM-lite) — 지금 돌릴 수 있는 2차 근거

주간 비음수 회귀(NNLS): `주간 전환수 ~ Σ 지면별 주간 노출`. 애플은 업사이즈 주를 빼고 BAU 24주로 적합.

| 캠페인 | 적합도 | 식별된 지면(95% 구간이 0을 안 포함) |
|---|---|---|
| 디즈니 플러스 | R²=0.594 | **커뮤니티 네이티브**(102~710원) · 홈 카드 |
| 애플 임팩트 | R²=0.589 | 홈 카드 |

- **대부분 지면은 식별 불가**다. 회귀는 "값을 준다"기보다 **어느 지면이 판정 가능해졌는지 알려주는 게이트**로 쓴다.
- 디즈니 네이티브는 하한 102원 vs 네트워크 134원 — **거의 걸쳐 있다.** 데이터가 조금만 더 쌓이면 판정 가능해진다.
- 🔴 **커뮤니티 보드는 구조적으로 가장 어렵다.** 집행률 91~94%에 물량이 크고 변동이 작아 회귀도 자연실험도 안 먹는다. **그런데 제휴 최대 노출 지면(애플 2.8억·디즈니 2.1억)이고 네트워크 eCPM도 166원으로 높다 — 가장 중요한데 가장 안 보인다.**

### 3-5. 📋 권고 — 「스팟 전부 끄기」 없이 가는 3단

**① 지금 배포 가능한 것부터 낸다**
A축(캠페인) 전면 + B축(비공유 지면) 지수. 여기에 배분 논쟁이 없다.

**② C축은 밴드로만 그리고 판정을 미룬다**
①클릭비례~②노출비례를 밴드로, 회귀 구간을 점선으로 얹는다. 밴드가 네트워크 선을 안 넘는 칸만 「판정」, 넘으면 「보류」.
지금 확정되는 건 **애플 × 커뮤니티 네이티브 = 제휴 우위** 한 칸이다.

**③ 변동을 싸게 만든다 — 스팟을 끄는 게 아니라 제휴끼리 스왑한다** ⭐
식별에 필요한 건 "꺼진 날"이 아니라 **물량 변동**이다.
→ **주 1~2일, 커뮤니티 보드·네이티브의 제휴 물량을 디즈니↔애플 사이에서 ±30% 스왑**한다.
- 총 제휴 물량이 안 줄고 **네트워크 물량도 안 건드린다** → 매출 손실 사실상 0
- 두 캠페인 노출이 반대로 움직이므로 **공선성이 깨져 회귀가 식별된다**
- 「스팟 전부 끄는 날」보다 훨씬 싸고, 보드까지 식별된다

**④ 측정 부착이 최종 해법 (병행)**
- **애플: sharedid** — 이미 진행 중(메일 스레드). 붙으면 지면별 액션 실측
- **디즈니: 혜택 페이지 클릭 로그에 지면 값 저장** — 별도 개발 필요(기식별 과제)
→ 붙는 지면부터 **밴드를 실측값으로 대체**한다. 화면 구조는 그대로 둔다.

**⑤ CVR 등급 테이블로 상태를 관리한다**

| 등급 | 근거 | 화면 표기 |
|---|---|---|
| **A 실측** | sharedid / 클릭 로그 | 원화 + 확정 |
| **B 식별** | 회귀 95% 구간이 0 제외 | 원화 + 구간 |
| **C 미식별** | — | **밴드만 · 원화 점추정 금지** |

---

## 4. 반드시 지킬 전제

| # | 전제 |
|---|---|
| **T1** | **A축은 실측, B·C축은 추정.** 화면에서 시각적으로 구분(추정 셀 빗금 + ▨) |
| **T2** | **일 단위 금지, 기본 주 단위.** 디즈니 일별 ROAS가 2.7~120.2%로 **44배 요동**(스팟 쏠림 + 전환 지연) |
| **T3** | **애플 시계열은 수수료 구간(3/4/6%)이 지배.** 7~9월 6% 적용일 18→9일 반토막. `bands` 음영 + 「BAU 기준」 토글. 회귀도 업사이즈 주 제외 |
| **T4** | **2026-09-17 랜딩 직행으로 클릭 정의가 바뀌었다**(도달률 25.6%→47.9%). 기준변경선 필수 · **지면마다 전환일이 다르다** · 회귀는 노출 기준이라 이 영향을 안 받는다 |
| **T5** | **최근 구간은 잠정.** 애플은 익월 22일 LockingDate 전까지 미확정 |
| **T6** | **타일에 증감 배지 금지.** 추이는 차트로만 |
| **T7** | **기준선은 `eCPM × 게재율`.** eCPM만 쓰면 보드에서 네트워크를 1.8배 과대평가한다 |
| **T8** | **애플은 기간을 묶지 않는다.** 업사이즈 포함 여부로 손익분기가 10% ↔ 121% 로 뒤집힌다. 기본값 최근 90일 |

---

## 5. 화면 구성

| # | 블록 | 내용 | 축 |
|---|---|---|---|
| 0 | 컨트롤 | 기간 · 해상도(주/월) · 업사이즈 포함·제외 · 잠정 표시 | |
| 1 | KPI 타일 | 캠페인별 R-eCPM · 총매출 · 소비 인벤토리 · 잠정 비중 | A |
| 2 | **캠페인 기여 랭킹** | 가로 막대(길이=매출, 수치=R-eCPM) | A |
| 3 | **시기별 추이** | 주간 R-eCPM 라인 + 업사이즈 음영 + 기준변경선 + 매출 스택 | A |
| 4 | **공유 지면 2개** | 보드·네이티브 — **손익분기 문턱값** + 클릭 기준 추정(하한) + 회귀점 + 판정 배지 | **C** |
| 5 | **비공유 지면** | 지면 × 캠페인 **기여지수 히트맵**(원화 없음) + 노출·클릭·CTR 실측 표 | **B** |
| 6 | 표·각주 | CSV·PNG + 출처·확정성·CVR 등급·상쇄 가정 각주 | |

---

## 6. ③번 — 「앞으로 어느 캠페인이 유리한가」

**예측이 아니라 「전망 + 시나리오」.** 캠페인이 2개뿐이라 학습 모델은 성립하지 않는다.

- `예상 매출 = 캠페인 R-eCPM × 예정 인벤토리 물량`. 물량은 인벤토리 탭 축(DAU × IPU)에서.
- 형상은 **검증된 엔진 재사용**: 전년 동월 형상 계수(백테스트 오차 3.0%) + 공휴일 계수.
- ⚠️ 애플은 전년 형상이 **업사이즈 스케줄에 좌우** → 수수료 구간 보정을 먼저 먹인다.
- ⛔ 계수는 **직전 1개월** + **최근 7일 런레이트로 검증**(기준선 전망 함정 재발 방지).
- **백테스트 필수** — 지난 3개월 전망 vs 실적 + MAPE.
- 신규 캠페인 프리뷰는 **Phase 4**.

---

## 7. 확장성

| 항목 | 방침 |
|---|---|
| 캠페인 추가 | **`aff_parse.py`의 `CAMPAIGNS`에 dict 한 줄.** 화면 코드 무수정 |
| 신규 캠페인의 축 배정 | 지면이 **공유/비공유 어느 쪽인지만** 매핑하면 A·B·C축에 자동 편입 |
| 지면명 표기 흔들림 | `검색키워드`/`검색 키워드` 둘 다 존재 → **별칭 정규화 테이블 한 곳으로** |
| 캠페인 색 | `_AFFPAL` 첫 두 색이 캠페인 정체성 색과 충돌하는 **미해결 이슈** |
| 5개 초과 시 | **상위 N + 「기타」 묶음** 규칙을 미리 정한다 |

---

## 8. 화면 이름·배치 — **보류** (사용자 지시)

기술 전제만 남긴다: 🔴 **네트워크 eCPM은 인벤토리 payload(`IV`)를 그대로 읽어 쓴다. 계산을 두 군데 두지 않는다.**

---

## 9. 로드맵

| Phase | 내용 | 선결 |
|---|---|---|
| **1** | 블록 1·2·3 (A축) + 블록 5 (B축 지수) | 없음 — **즉시 착수 가능** |
| **2** | 블록 4 (C축 밴드 + 판정/보류 배지) + 회귀 파이프라인 + CVR 등급 테이블 | Phase 1 |
| **3** | 스왑 실험(§3-5③) 결과 반영 → 식별되는 칸부터 판정 전환 | 운영 협의 |
| **4** | 측정 부착(sharedid·디즈니 클릭 로그) → 밴드를 실측으로 대체 | 개발 |
| **5** | 전망 + 백테스트 + 신규 캠페인 프리뷰 | Phase 1 검증 |

**데이터 작업 (Phase 1 전)**
1. 스플래시·proxy 지면 제외 플래그를 `aff_data.js`에 명시
2. 지면을 **공유/비공유**로 분류하는 테이블 신설 (현재 공유 = 커뮤니티 보드·네이티브)
3. 지면별 **랜딩 직행 전환일** 테이블 신설(T4)
4. **CVR 등급 테이블**(지면 × 캠페인 × 등급 × 값 × 근거일자) 신설

---

## 10. 남은 확인

1. **디즈니 공유 지면(보드·네이티브) 스위치백을 걸 수 있는가** — 미결은 디즈니 2칸뿐이고,
   네이티브는 29% vs 33%로 거의 붙어 있어 8주면 판정이 선다. **애플은 이미 답이 나서 흔들 필요 없다**
2. 디즈니 혜택 페이지 클릭 로그에 지면 값 저장 — 개발 요청을 넣을지
3. 전망 기간 — 1개월 / 3개월 / 분기
4. 화면 이름·배치 (보류 중)
