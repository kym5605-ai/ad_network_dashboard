#!/usr/bin/env python3
"""스킬 프롬프트 — 대시보드 콘솔과 팀원 복사용 명령의 **단일 정본**.

여기를 고치면 두 곳이 같이 따라온다:
  1) aff_server.py 가 import 해서 콘솔 세션에 넣는다
  2) build_prompts.py 가 index.plain.html 의 `const AFFPROMPT=` 한 줄로 심는다
     → 배포본을 보는 팀원의 복사 버튼도 같은 문구를 받는다

⚠️ 두 벌로 복사해 두지 말 것. 예전에 서버 프롬프트와 카드 설명이 갈라져
   "디즈니 H열을 채운다/안 채운다"가 문서마다 달랐던 사고가 있었다.
"""
import os

DAILY = {'apple': 'apple-daily', 'disney': 'disney-daily'}

# 데일리가 매일 제자리 갱신하는 정본 파일(시놀로지 5.제휴 기준 상대경로 패턴).
# ⚠️ 파일명을 바꾸지 않는다 — 사용자가 NAS 원본 자리에 그대로 덮어 넣어야 하므로 이름이 흔들리면 안 된다.
# ⚠️ 앞에 `*` 가 붙어 있다 — 옛 이름(`report_…_원본_260819.xlsx`)과
#    새 이름(`Apple_…_내부용_건우_260921.xlsx`)을 한 패턴으로 같이 잡는다.
_APPLE_BOOK = ("Apple Affiliate/Impact/2026년/하반기 BAU 캠페인 (7:1~12:31)/광고리포트/"
               "*Apple_2026_하반기_BAU_내부용_*.xlsx")
_APPLE_NEW = "Apple_2026_하반기_BAU_내부용_<담당자>_<YYMMDD>.xlsx"
# ⚠️ 폴더가 재편됐다(2026-09-22): `claude(8:10~)` → `내부/`. 상위 폴더명도 `~`가 `-`로 바뀌어
#    와일드카드로 받는다. 「아카이브」 폴더의 옛 파일은 잡히면 안 되므로 `내부/`를 명시한다.
_DISNEY_BOOK = ("Disney+ Affiliate/12개월 할인*/광고 리포트/내부/"
                "*Disneyplus_2026_내부용_*.xlsx")
_DISNEY_NEW = "Disneyplus_2026_내부용_<담당자>_<YYMMDD>.xlsx"
# ~ 로 둔다 — 시놀로지 마운트 이름은 팀 공유 폴더명에서 오므로 누구 맥이든 같다.
_ROOT = "~/Library/CloudStorage/SynologyDrive-AD_Monetization/5.제휴"
# 디즈니 구독 건수의 원천. 한 파일 = 하루치이고, 헤더 뺀 행 수가 그대로 구독 건수다.
_DISNEY_DAILY = ("Disney+ Affiliate/12개월 할인(2026.02.10~2027.02.28)/"
                 "데일리 성과 리포트/26년 N월/everytime_daf_responder_YYYYMMDD000000_UTC.csv")
# 주간 KPI 관리 시트(.osheet)가 약 7일 간격으로 새로 생기는 자리.
_KPI_DIR = "~/Library/CloudStorage/SynologyDrive-AD_public/광고실적 데이터"

# 데일리 프롬프트 공통 규칙. 무인이 아니라 사용자가 보고 있는 콘솔에서 도는 전제다.
# 어느 스킬이든 맨 앞에 붙는다. 팀원이 Claude 데스크탑 앱에 붙여넣었을 때
# **작업을 시작하기 전에** 준비물부터 확인하고 안내하도록 만드는 부분이다.
_PREREQ = """
[0단계 — 시작 전 준비물 점검. 이걸 먼저 하고 나서 작업에 들어간다]
아래를 하나씩 확인한다. **빠진 게 있으면 작업을 시작하지 말고**, 무엇을 어떻게 준비해야 하는지
사용자에게 알려준 뒤 기다린다. 추측으로 우회하지 않는다.
{prereq}
확인이 끝나면 "준비물 확인 완료" 한 줄과 함께 본 작업을 시작한다.
"""

_DAILY_COMMON = f"""
[데일리 실행]
- 대상 기간은 {{from}} ~ {{to}}. 오늘은 집계 진행 중이라 넣지 않는다.
- 원본은 사용자에게 첨부를 요청하지 말고 아래 경로에서 직접 연다. 루트는 {_ROOT} 다.
- **원본 파일을 덮어쓰지 않는다.** 원본은 읽기만 하고, **같은 폴더에 새 이름으로 만든다.**
  - 이름 규칙: 「원본명에서 날짜·"원본"을 뗀 것」 + 「_담당자」 + 「_YYMMDD」 + `.xlsx`
    예) `Apple_2026_하반기_BAU_내부용_건우_260921.xlsx`
        `Disneyplus_2026_내부용_유니_260921.xlsx`
  - 담당자 이름을 모르면 **물어본다.** 임의로 정하지 않는다.
  - **꼬리는 덧붙이지 말고 교체한다.** `…_건우_260921` 를 베이스로 삼았다면
    새 파일은 `…_유니_260928` 이지 `…_건우_260921_유니_260928` 이 아니다.
- **베이스는 항상 같은 패턴 중 수정시각이 가장 최신인 파일**이다. 옛 파일을 열면
  사람이 손으로 넣어 둔 값(예: 디즈니 구독 건수 H열)이 통째로 날아간다.
- adsystem은 Chrome으로 연다. 로그인 화면이 뜨면 작업을 멈추고 콘솔로
  "adsystem SSO 로그인을 해주세요"라고 요청한 뒤 사용자의 답을 기다린다. 우회하지 않는다.
- 다 끝나면 **만든 파일의 전체 경로**를 적고, 신규 입력 구간 + 소급 변경(전/후)을 표로 보고한다.
  옮기는 단계는 없다 — 공유 폴더에 새 파일로 이미 만들어 둔 것이다.
- **완료 신호**: 모든 작업이 끝나 더 할 일이 없을 때만, 보고 맨 마지막에 `[AFF-DONE]` 한 줄을
  단독으로 출력한다. 대시보드가 이 줄을 보고 「대시보드에 반영」 버튼을 띄운다.
  중간에 사용자에게 확인을 구하거나 로그인을 기다릴 때는 절대 출력하지 않는다.
"""


_NOTION_COMMON = """
[대시보드 「주간보고 → 노션」 버튼]
- **노션 페이지 링크는 매주 바뀐다.** 시작하자마자 콘솔로
  "이번 주 주간보고 **페이지** 링크를 붙여넣어 주세요"라고 묻고 **답을 받을 때까지 기다린다.**
  링크를 추측하거나 검색해서 고르지 않는다 — 지난 주차 페이지는 스냅샷이라 고치면 안 된다.
- **앵커(`#블록id`)나 `?source=` 같은 꼬리가 붙어 있으면 떼고 페이지 단위로 연다.**
  표는 페이지 안의 제목 구조를 훑어 찾으므로 블록 하나만 열면 주변 표를 못 읽는다.
- 지정된 표 밖은 절대 건드리지 않는다. 수정 전후로 페이지를 받아 변경 범위를 대조해 보고한다.
- 엑셀은 이미 채워져 있다는 전제다. 비어 있으면 멈추고 「데이터 업데이트」를 먼저 돌리라고 알린다.
"""

SKILLS = {
    'apple-daily': {
        'label': '애플 임팩트 데이터 업데이트',
        'desc': 'adsystem 광고수치 + Impact 전환을 내부용 엑셀에 채운다(노션 제외)',
        'prompt': ('/apple-weekly-report **① 엑셀 단계만 한다. ② 노션 주간 보고는 하지 않는다.**'
                   + _DAILY_COMMON
                   + f'- 읽어올 원본: `{_APPLE_BOOK}` 중 수정시각이 가장 최신인 것.\n'
                   + '- 건별 상품 광고비는 일 노출 10만 이상을 집행일로 자동 판정한다. '
                     '판정이 애매한 날만 콘솔로 물어본다.\n'
                   + '- Impact 전환은 소급 확정되므로 증분만 붙이지 말고 기존 구간 전체를 '
                     'API로 다시 받아 대조한다.\n'),
    },
    'disney-daily': {
        'label': '디즈니 플러스 데이터 업데이트',
        'desc': 'adsystem 지면별 노출·클릭 + 구독 건수를 내부용 엑셀에 채운다(외부용·시트·노션 제외)',
        'prompt': ('/disney-weekly-report **① 내부용 엑셀만 한다. '
                   '외부용 xlsx·구글시트·노션 5-2 표는 전부 하지 않는다.**'
                   + _DAILY_COMMON
                   + f'- 읽어올 원본: `{_DISNEY_BOOK}` 중 수정시각이 가장 최신인 것.\n'
                   + '- **구독 건 수((통합) H열)도 채운다.** 2026-09-14 지시로 바뀌었다 — '
                     '예전 "사용자가 직접 넣으니 건드리지 마"는 폐기됐다.\n'
                   + f'  - 원천: `{_ROOT}/{_DISNEY_DAILY}`\n'
                   + '  - 경로의 `26년 N월`에서 N은 **대상 날짜의 월**이다(9/14치 → `26년 9월`). 월이 걸치면 폴더 두 개를 다 본다.\n'
                   + '  - **한 파일 = 하루치**이고 파일 안에 다른 날짜가 섞이지 않는다. '
                     '**헤더 뺀 행 수를 그대로** H열에 넣는다. **중복 행도 그대로 센다** '
                     '(2026-08-26 확정 기준 — 8/18~20에 완전 중복 37건이 있으나 빼지 않는다). '
                     '기준을 임의로 바꾸지 말고 이상하면 물어본다.\n'
                   + '  - **I·J열(비누랩스 매출·순수익)은 `=H*10000` 수식이라 건드리지 않는다.** '
                     'H만 넣으면 따라온다.\n'
                   + '  - **넣기 전 자가검증**: H가 이미 찬 최근 3~5일을 같은 방식으로 세어 값이 맞는지 '
                     '먼저 확인하고, 어긋나면 넣지 말고 보고한다.\n'
                   + '  - **CSV는 하루 늦게, 그리고 밀려서 들어온다.** 대상 기간이라도 파일이 없는 날은 '
                     '**채우지 말고 비워 둔 채 보고**한다.\n'
                   + '  - 파일명이 NFD라 `ls | grep`이 안 먹을 수 있다 → `os.listdir` + '
                     'unicodedata.normalize("NFC", ...) 로 읽는다. `..._UTC 2.csv`처럼 ` 2`가 붙은 '
                     '사본이 섞이니 날짜는 파일명의 `YYYYMMDD`로 파싱하고 같은 날짜를 두 번 넣지 않는다.\n'
                   + '- 앱만 집계한다((앱)노출수·(앱)클릭수). 웹은 제외 — 애플과 다르다.\n'
                   + '- **「혜택 페이지」 탭은 statdaily에 없다.** '
                     'https://admin.everytime.kr/benefit/stat?id=134 를 Chrome으로 열어 '
                     '표의 **「이벤트수 (회)」** 조회·받기·클릭을 B·C·D에 넣는다 '
                     '(왼쪽 「회원수 (명)」 아니다 — 스킬 문서의 대조표로 열을 먼저 확인할 것). '
                     'E·F·G는 수식이니 건드리지 않는다. 7/27부터 비어 있으니 대상일까지 소급해 채운다. '
                     '이 사이트는 OTP라 자동 로그인이 안 된다 — 로그인 화면이 뜨면 멈추고 콘솔로 '
                     '"admin.everytime.kr 혜택 통계에 로그인해 주세요"라고 요청하고 기다린다.\n'),
    },
    'apple-notion': {
        'label': '애플 임팩트 주간보고 → 노션',
        'desc': '엑셀 값을 노션 주간 보고 5-1 Apple Impact 표(월별·일별)에 반영',
        'prompt': ('/apple-weekly-report **② 노션 단계만 한다. ① 엑셀은 건드리지 않는다.**\n'
                   + '주간 보고 업데이트 하자. 보고 대상 주는 {from} ~ {to} 입니다.'
                   + _NOTION_COMMON
                   + '- 요약 문구("Impact: N월 마감 …")는 사용자가 직접 고친다. '
                     '금액이 바뀌면 계산해서 알려만 주고 건드리지 않는다.\n'),
    },
    'disney-notion': {
        'label': '디즈니 플러스 주간보고 → 노션',
        'desc': '노션 주간 보고 5-2 Disney+ 표 갱신 (구글시트 단계는 하지 않음)',
        'prompt': ('/disney-weekly-report **③ 노션 단계만 한다. '
                   '① 엑셀과 ② 구글시트는 건드리지 않는다.**\n'
                   + '주간 보고 업데이트 하자. 보고 대상 주는 {from} ~ {to} 입니다.'
                   + _NOTION_COMMON
                   + '- 구독 건 수는 **엑셀 (통합) H열 = 노션 표**가 같아야 한다. '
                     '다르면 고치지 말고 차이를 보고한다.\n'),
    },
    'disney-sheet': {
        'label': '디즈니 플러스 구글시트 갱신',
        'desc': 'Everytime Impact Report(파트너 공유용) A~K열을 엑셀 값으로 채운다',
        'prompt': ('/disney-weekly-report **② 구글시트 단계만 한다. '
                   '① 엑셀과 ③ 노션은 건드리지 않는다.**\n'
                   '임팩트 리포트 시트 채워줘. 대상 기간은 {from} ~ {to} 입니다.\n'
                   '- 대상: Everytime Impact Report '
                   '`1tWBvTlr2IK2Bk2XSERBu-lV4z_Y_t60_79ccV4SS1Q8` (gid=0)\n'
                   '- **A열 날짜를 읽어 실제 마지막 입력 행을 먼저 확인**한다. 문서의 행 매핑을 믿지 않는다.\n'
                   '- **A~K 전부 채운다.** A=날짜(`2026.08.01` 형식) · B=`Korea` · C=`Vinulabs` · '
                   'D=`DISNEY_EVERYTIME_KR_PROMO_CMPGN` · F=공란 · K=`standalone`.\n'
                   '- **광고 실적(노출·클릭·집행금액)이 0인 날은 넣지 않는다.** 구독수만 있고 '
                   'statdaily 수치를 아직 안 받은 날이 있다 — 그런 날은 건너뛰고 보고한다.\n'
                   '- Chrome으로 시트를 연다. 로그인 화면이 뜨면 콘솔로 요청하고 기다린다.\n'
                   '- 끝나면 넣은 행 범위와 값을 표로 보고하고, 마지막 줄에 `[AFF-DONE]`을 단독 출력한다.'),
    },
    'weekly': {
        'label': '애플 임팩트 주간 리포트',
        'desc': 'adsystem·Impact에서 긁어 리포트 엑셀을 채우고 노션 주간 보고까지 갱신',
        'prompt': '/apple-weekly-report 대시보드에서 보던 기간은 {from} ~ {to} 입니다. 애플 리포트 업데이트 하자',
    },
    'monthly': {
        'label': '애플 임팩트 월 마감',
        'desc': '전월 전체를 재대조해 소급 확정치 반영 + 월 합계·ROAS 산출 + 노션 반영',
        'prompt': '/apple-monthly-close 애플 월마감 하자. 마감 대상 기간은 {from} ~ {to} 입니다',
    },
    'dweekly': {
        'label': '디즈니 플러스 주간 리포트',
        'desc': 'statdaily + 혜택 통계 수집 → 내부용·외부용 엑셀 → 구글시트 → 노션 5-2 표',
        'prompt': ('/disney-weekly-report 디즈니 리포트 업데이트 하자. 대상 주간은 {from} ~ {to} 입니다.\n'
                   + f'- 원본은 첨부를 요청하지 말고 직접 연다. 루트는 {_ROOT} 다.\n'
                   + f'  내부용: `{_DISNEY_BOOK}` 중 수정시각이 가장 최신인 것.\n'
                   + '  외부용: 같은 폴더의 `report_Disneyplus_*_외부용.xlsx` 중 가장 최신인 것.\n'
                   + '- **원본은 읽기만 한다.** 사본을 산출물 폴더에 만들어 거기서 작업하고, '
                     '파일명은 원본과 같게 유지한다. NAS로 옮기는 건 사용자가 한다.\n'
                   + '- 「혜택 페이지」 탭은 statdaily가 아니라 '
                     'https://admin.everytime.kr/benefit/stat?id=134 에서 가져온다. '
                     '표의 **「이벤트수 (회)」** 조회·받기·클릭을 B·C·D에 넣는다(「회원수 (명)」 아님). '
                     '내부용·외부용 둘 다. **이미 값이 있는 날짜는 그대로 두고 빈 날짜만 채운다.** '
                     'OTP 때문에 로그인은 사용자가 미리 해둔다 — 로그인 화면이 뜨면 멈추고 요청한다.\n'
                   + '- **(통합) H·I·J열(구독 건 수·매출·순수익)은 건드리지 않는다.** '
                     '사용자가 데일리 CSV로 직접 넣는 값이다.\n'
                   + '- adsystem은 Chrome으로 연다. 로그인 화면이 뜨면 멈추고 콘솔로 '
                     '"adsystem SSO 로그인을 해주세요"라고 요청한 뒤 기다린다. 우회하지 않는다.\n'
                   + '- **④ 메일 초안**: 엑셀이 채워지면 '
                     '`~/.claude/skills/disney-weekly-report/scripts/outlook_draft.py` 로 '
                     '아웃룩 임시 보관함에 초안을 만든다. **수신자는 kw.kim@vinulabs.com(본인)** 이고 '
                     '이번 회차 **외부용 xlsx를 첨부**한다. ⛔ **보내지 않는다 — 저장까지가 전부다.** '
                     '정성 코멘트는 비워 두고 수치 해석 한 줄만 힌트로 적는다'
                     '(대외 메일이라 물량·소재 같은 약속을 쓰지 않는다).\n'
                   + '- 다 끝나면 **① 산출물 경로 ② 덮어 넣을 NAS 원본 경로**를 나란히 적고, '
                     '신규 입력 구간 + 소급 변경(전/후)을 표로 보고한다.\n'),
    },
    'cmonthly': {
        'label': '커머스 월 마감',
        'desc': 'CPS·모바일교환권 정산 검증 + ERP 입력값 + 애드랩 업로드 파일',
        'prompt': '/commerce-monthly-close 커머스 월마감 하자. 정산월은 {from} ~ {to} 입니다',
    },
    'nweekly': {
        'label': '네트워크 주간보고',
        'desc': '대시보드 원본에서 MTD YoY를 뽑아 노션 임베드 블록을 다시 만들고 3장 본문까지 작성',
        'prompt': '/network-weekly-report 주간보고 네트워크 영역 갱신하자. 보고 대상 주는 {from} ~ {to} 입니다',
    },
    'nsettle': {
        'label': '네트워크 정산 · 월마감',
        'desc': '매체 7곳 정산 금액 확인 → ERP 입력값 + 월마감 공통 업무 정리',
        'prompt': '/network-settlement-close 네트워크 광고 정산 하자. 정산월은 {from} ~ {to} 입니다',
    },
    # 커머스 일별 원장 — KPI 기입의 선행 단계. 원장은 여기서만 채우고, KPI 시트로 옮기는 건
    # kpi-weekly가 한다(2026-09-14 분리). 한 스킬이 두 시트를 쓰면 어디서 틀렸는지 못 가른다.
    'cledger': {
        'label': '커머스 일별 원장 갱신',
        'desc': '묘철님 전달자료 주간 xlsx → 커머스 현황 시트 일자별 항목 + 당월 매출 산출',
        'prompt': ('/commerce-weekly-ledger 커머스 원장 갱신 하자. 대상 기간은 {from} ~ {to} 입니다.\n'
                   f'- 원천은 `{_ROOT}/비누커머스/묘철님 전달자료/에브리타임을 통해 발생한 매출_*.xlsx`. '
                   '**당월에 걸리는 파일을 전부 훑어** 날짜별로 모은다 — 한 파일만 보면 그 주만 들어간다.\n'
                   '- 파일명 날짜는 데이터 기간이 아니라 **그 주가 끝난 다음 날**이다. '
                   "안의 `날짜일` 값으로 기간을 판단한다.\n"
                   '- 파일명 검색에 한글 glob을 쓰지 않는다(시놀로지는 NFD). '
                   "os.listdir 후 unicodedata.normalize('NFC', name)로 비교한다.\n"
                   '- 열 매핑: 에브리타임 알파→알파 · 알파-캐러셀→알파-캐러셀 · '
                   '쇼핑탭→**쇼핑탭 통합** · 혜택탭→**혜택탭 통합**. 이름이 서로 다르니 반드시 이 표대로.\n'
                   '- 시트의 날짜 열을 읽어 **일자를 맞춰** 넣는다. 행 위치를 하드코딩하지 않는다.\n'
                   '- ⛔ **이미 값이 있는 날짜는 고치지 않는다.** 대조만 하고 다르면 차이를 보고한다. '
                   '빈 날짜만 채운다. 입력 범위(대략 B10:N26) 밖은 건드리지 않는다.\n'
                   '- ⛔ 시놀로지 오피스에서 **방향키·PageUp/PageDown 금지**(셀에 문자열로 입력된 사고 있음). '
                   '스크롤은 스크롤바 드래그로만, 셀 클릭은 값을 쓸 그 셀에만 한다.\n'
                   '- 끝나면 **당월 매출(당월 열 10행)을 읽어 크게 보고**한다 — '
                   '이 값이 주간 KPI 시트의 커머스 CPS 칸으로 간다. 다시 계산하지 않는다.\n'
                   '- 최소 보장 수수료(B2)는 말일에 차액으로 가산된다. 주간 시점에는 반영하지 않는다.\n'
                   '- 전/후 대조표와 **「지정 범위 밖 변경 0건」** 확인을 보고한다.\n'),
    },
    # 주간 KPI 시트 기입 — 애플·디즈니·네트워크·커머스를 가로지르므로 「종합」 열에 둔다.
    # 여러 팀이 함께 쓰는 정본 시트라 규칙이 유독 빡빡하다(지정 셀 외 금지·확인 후 기입).
    'kpi-weekly': {
        'label': '주간 KPI 매출 기입',
        'desc': 'KPI 관리 시트 매출_2026 탭에 담당 BM 5개 항목의 당월 매출을 채운다',
        'prompt': ('/weekly-kpi-revenue 주간 KPI 매출 기입 하자. 보고 대상 주는 {from} ~ {to} 입니다.\n'
                   '- **기한은 매주 화요일 18:00**(수요일 주간보고 전)이다.\n'
                   f'- KPI 파일은 `{_KPI_DIR}` 에서 **파일명 끝 8자리 날짜가 가장 큰 것**을 고른다. '
                   'mtime으로 고르지 않는다 — 지난 주 파일을 열어보면 mtime이 갱신된다.\n'
                   '- **이번 주 파일이 아직 없으면 멈추고 알린다.** 지난 주 파일에 쓰지 않는다.\n'
                   '- ⛔ **파일을 스스로 열지 않는다(2026-09-14 지시).** 후보 3개를 '
                   '파일명·파일명날짜·수정시각·크기 표로 제시한 뒤, 콘솔로 '
                   '"이번 주 KPI 파일이 이게 맞으면 아래 「파일 첨부」 버튼으로 올려주세요"라고 '
                   '요청하고 **기다린다.** 사용자가 올린 파일을 정본으로 삼는다 — '
                   '내가 고른 후보와 달라도 사용자 것을 따른다. 첨부 없이는 다음 단계로 가지 않는다.\n'
                   '- `.osheet`는 표준 도구로 못 읽는다. 첨부는 **어느 파일인지 확정하는 용도**이고, '
                   '값 읽기·쓰기는 시놀로지 오피스 라이브 화면에서 한다(CSV로 받으면 셀 주소를 잃는다).\n'
                   '- 파일명 검색에 한글 glob을 쓰지 않는다(시놀로지는 NFD). '
                   "os.listdir 후 unicodedata.normalize('NFC', name)로 비교한다.\n"
                   '- 공유 링크는 **하드코딩하지 않는다.** 매주 새 파일이라 매주 바뀐다.\n'
                   '- ⛔ **지정된 셀 외에는 어떤 셀도 건드리지 않는다.** 여러 팀이 함께 쓰는 정본이다. '
                   '자동 계산 칸(194·195·197·총합계)에 손으로 넣지 않는다.\n'
                   '- ⛔ 시놀로지 오피스에서 **방향키·PageUp/PageDown 금지**(셀에 문자열로 입력된 사고 있음). '
                   '스크롤은 스크롤바 드래그로만, 셀 클릭은 값을 쓸 그 셀에만 한다.\n'
                   '- 월 열을 하드코딩하지 않는다. 머리 행을 읽어 당월 열을 찾는다(실측: 8월=J → 9월=K).\n'
                   '- **쓰기 전에 5개 항목을 표로 제시하고 사용자 확인을 받는다.** 확인 없이 쓰지 않는다. '
                   '값이 불확실하면 0으로 채우지 말고 빈칸 + 사유로 남긴다.\n'
                   '- 버즈빌·시놀로지 로그인 화면이 뜨면 멈추고 콘솔로 요청한 뒤 기다린다. 우회하지 않는다.\n'
                   '- ⛔ **커머스 일별 원장은 여기서 채우지 않는다.** 커머스 현황 시트(tid=44)의 '
                   '당월 열 10행 값을 **읽기만** 한다(셀 클릭 금지). 원장이 어제까지 안 차 있으면 '
                   '멈추고 "Weekly · 커머스의 「일별 원장 갱신」을 먼저 돌려주세요"라고 알린다.\n'
                   '- 끝나면 전/후 대조표와 **「지정 셀 밖 변경 0건」** 확인을 보고한다.\n'),
    },
    # 제휴+네트워크를 가로지르는 유일한 카드. 어느 브랜드에도 속하지 않아 목록 맨 아래에 둔다.
    # period는 'lastmonth' — 1영업일에 돌리면 전월 1일~말일이 그대로 대상 월이 된다.
    'preclose': {
        'label': '재무팀 가마감 매출 회신',
        'desc': '제휴 4 + 네트워크 7 + 오퍼월 당월 예상 매출을 한 표로 만들어 회신값을 낸다',
        'prompt': ('/revenue-preclose 가마감 하자. 대상 월은 {from} ~ {to} 입니다.\n'
                   '- 확정이 아니라 **예상치 회신**이다. Impact는 (A)직전월 차액 / (B)당월 예상 / '
                   '(A+B) 3단으로 나눠 적는다.\n'
                   '- 환율은 **서울외국환중개 해당 월 환율**을 쓴다. 네이버증권 전월 말일 기준은 폐기됐다.\n'
                   '- 네트워크 수익은 macro5 통합 탭 / 자체 네트워크 대시보드의 플랫폼별 수익에서 읽는다. '
                   '대시보드가 대상 월 말일까지 차 있지 않으면 멈추고 콘솔로 알린다 — 추정하지 않는다.\n'
                   '- 버즈빌은 네트워크가 아니라 **오퍼월** 줄이다.\n'
                   '- 값이 없는 항목은 0으로 채우지 말고 **빈칸 + 사유**로 남긴다.\n'
                   '- 자료가 없으면 어디서 받아야 하는지와 함께 콘솔로 요청하고 기다린다.\n'
                   '- 네트워크 금액은 대시보드가 아니라 **macro5(`Adnetwork_Data_macro5_*.xlsx` 통합 탭)**'
                   '에서 읽는다. 대시보드는 D-2라 말일이 비어 있다.\n'
                   '- **애드랩 입력까지가 이 업무다.** 「최종 정산 금액」(네트워크6·오퍼월1·제휴5)과 '
                   '「일 매출 등록」 CSV(디즈니+·비누커머스·파워링크)를 챙긴다. 디즈니 CSV는 '
                   '`~/.claude/skills/revenue-preclose/scripts/make_adlab_csv.py disney YYYY-MM`으로 만든다. '
                   '**업로드 버튼은 사람이 누른다 — 파일만 만들고 경로를 알려준다.**\n'
                   '- 환율은 자동 수집이 안 되니 사용자에게 해당 월 서울외국환중개 월평균을 묻는다.\n'
                   '- 끝나면 회신 메일을 **컴포즈 창 형태 아티팩트**로 만들고, 사람이 할 일'
                   '(메일 회신 · 애드랩 입력 · CSV 업로드 · 시트 2칸 기재 · 선행 요청) 체크리스트를 낸다.\n'),
    },
    'inv-daily': {
        'label': '인벤토리 데이터 갱신',
        'desc': 'adsystem 지면표·캠페인 노출을 증분 수집해 인벤토리 탭을 최신화하고 배포한다',
        'prompt': (
            '인벤토리 탭 데이터를 갱신한다. 작업 폴더는 `~/ad_network_dashboard` 다.\n'
            '\n[전체 흐름] 수집(Chrome) → 분할 → 빌드 → 주입 → 배포\n'
            '\n'
            '**0. 어디까지 차 있는지 먼저 본다.** 이미 있는 날짜를 다시 긁지 않는다.\n'
            '```bash\n'
            'cd ~/ad_network_dashboard\n'
            'python3 - <<\'PY\'\n'
            'import csv\n'
            'for f in ("direct_position_full.csv","direct_position_split.csv"):\n'
            '    rows=list(csv.reader(open(f)))[1:]\n'
            '    d=sorted(set(r[0] for r in rows if r and r[0][:2]=="20"))\n'
            '    print(f, d[-1])\n'
            'PY\n'
            '```\n'
            '수집 대상 = **그 다음 날 ~ 어제**. 오늘은 집계 중이라 넣지 않는다.\n'
            '이미 어제까지 차 있으면 **수집을 건너뛰고** 그 사실을 보고한 뒤 4번(빌드)부터 한다.\n'
            '\n'
            '**1. 수신 서버를 띄운다.** javascript_tool 응답이 ~1,000자에서 잘리므로 결과를 이리로 POST 한다.\n'
            '```bash\n'
            'cd ~/ad_network_dashboard && nohup python3 collect_srv.py > /tmp/collect_srv.log 2>&1 &\n'
            '```\n'
            '끝나면 반드시 `pkill -f collect_srv.py` 로 내린다.\n'
            '\n'
            '**2. Chrome 으로 수집한다.** adsystem 에 로그인된 탭에서 `javascript_tool` 로 fetch 한다.\n'
            '  - 지면별 일일 노출표: `/statpositionimpressiondaily/<시작>/<종료>` — **1회 요청**이면 끝난다.\n'
            '    받은 표를 `direct_position_full.csv` 형식(첫 열 날짜)으로 만들어 `/put?f=position_incr.csv` 로 넘긴다.\n'
            '  - 지면↔postfix 매핑: `/checksetting` → `/put?f=checksetting.html` (덮어쓰기: 먼저 `/reset`)\n'
            '  - 캠페인별 일별 노출: `/checksetting` 에서 **현재 집행 중인 캠페인 id** 를 뽑아\n'
            '    각각 `/statdaily/<id>` 를 훑고 한 줄에 JSON 하나씩 — 키는 id · n(캠페인명) ·\n'
            '    r(=[[날짜,앱노출,웹노출], ...]) 이다. `.collect/daily6.jsonl` 의 기존 줄과 같은 형식이면 된다.\n'
            '    `/put?f=daily<다음번호>.jsonl` 로 넘긴다. **`.collect/daily*.jsonl` 중 안 쓰인 번호를 골라 쓴다**\n'
            '    — 기존 파일에 덮어쓰면 과거 수집분이 날아간다.\n'
            '    ⚠ 동시 20장 정도로 나눠 돈다. 한 장이 ~1.2MB 라 한꺼번에 열면 탭이 멈춘다.\n'
            '    ⚠ **캠페인명 앞 8자리는 집행일이 아니라 생성일**이다. 날짜로 거르지 말 것.\n'
            '\n'
            '**3. 지면표 증분을 본 파일에 합친다.** 날짜 기준으로 같은 날이 있으면 새 값으로 교체한다.\n'
            '\n'
            '**4. 빌드·주입·배포.** 여기부터는 Chrome 이 필요 없다.\n'
            '```bash\n'
            'cd ~/ad_network_dashboard\n'
            'python3 split_build.py          # → direct_position_split.csv + .collect/recon.txt\n'
            'python3 build_inventory.py      # GA4 자동 동기화 포함 → inventory_proto.html\n'
            'python3 inject_inventory.py     # → index.plain.html 의 INV_START~INV_END 교체\n'
            "PW='Vinulabs1!' python3 encrypt.py && git add index.html && "
            'git commit -m "인벤토리 데이터 최신화" && git push\n'
            '```\n'
            '\n'
            '**5. 검증하고 보고한다.**\n'
            '  - `.collect/recon.txt` 의 잔차를 확인한다. 잔차는 매핑 못 한 캠페인이라 직광고로 흡수된다.\n'
            '    ⚠ **홈 배너(Web)·홈 카드(Web) 두 지면은 원래 대조가 안 맞는다**(전체의 1.3%, 미해결).\n'
            '      그 둘 말고 새로 어긋난 지면이 생겼을 때만 보고한다.\n'
            '  - 보고: 수집한 날짜 구간 · 캠페인 수 · 각 CSV 의 마지막 날짜 · 배포 커밋 해시\n'
            '\n'
            '⚠ **원본 CSV 를 덮어쓰기 전에 `_before` 사본을 남긴다.** 수집이 반쪽이면 되돌려야 한다.\n'
        ),
    },
}


# 완료 신호 — 콘솔이 세션을 닫는 신호다. 이미 문구에 있는 스킬(데일리)은 건너뛴다.
# ⚠️ 서버에서 런타임에 붙이던 것을 여기로 옮겼다. 안 그러면 팀원 복사본에만 빠진다.
_DONE_LINE = ('\n- **완료 신호**: 모든 작업이 끝나 더 할 일이 없을 때만, 보고 맨 마지막에 '
              '`[AFF-DONE]` 한 줄을 단독으로 출력한다. 대시보드가 이 줄을 보고 세션을 닫아 '
              '다음 스킬이 이어서 돌 수 있게 한다. 중간에 사용자에게 확인을 구하거나 '
              '로그인을 기다릴 때는 절대 출력하지 않는다.\n')
for _sk in SKILLS.values():
    if '[AFF-DONE]' not in _sk['prompt']:
        _sk['prompt'] += _DONE_LINE


# ── 준비물 ────────────────────────────────────────────────────────────────────
#   팀원이 Claude 데스크탑 앱에 명령을 붙여넣었을 때, 본 작업 전에 이걸 먼저 점검한다.
#   콘솔(내 맥)에서는 이미 갖춰져 있어 대부분 즉시 통과한다.
_P_SYNO = ("- **시놀로지 드라이브 동기화** — `~/Library/CloudStorage/` 아래에 "
           "`SynologyDrive-AD_Monetization` 이 보여야 한다. 없으면 Synology Drive 앱에서 "
           "해당 팀 폴더를 동기화하도록 안내한다.\n"
           "  ⚠️ 파일이 '온디맨드 스텁'이면 열 때 깨진다. 읽히는지 실제로 한 번 열어 확인한다.")
_P_CHROME = ("- **Chrome에 adsystem 로그인 + Claude in Chrome 확장 연결** — statdaily를 직접 읽어야 한다. "
             "확장이 없으면 이 작업은 시작할 수 없다. 로그인 화면이 뜨면 멈추고 요청한 뒤 기다린다.")
_P_IMPACT = ("- **Impact 자격증명**(선택) — `~/.config/impact/credentials` 가 있으면 액션·판매금액을 "
             "API로 바로 받는다. 없으면 사용자에게 impact.com export 파일을 요청한다.")
_P_OTP = ("- **admin.everytime.kr 혜택 통계 로그인** — OTP라 자동 로그인이 안 된다. "
          "미리 로그인해 두었는지 확인하고, 안 돼 있으면 먼저 로그인을 요청한다.")
_P_NOTION = ("- **노션 MCP 연결 + 주간보고 페이지 편집 권한** — 연결이 없으면 표를 못 고친다. "
             "그리고 **이번 주 페이지 링크는 사용자에게 받아야 한다**(매주 바뀐다).")
_P_GDRIVE = "- **구글 드라이브 MCP 연결** — Everytime Impact Report 시트를 열어야 한다."
_P_NAME = "- **담당자 이름** — 만들 파일명에 들어간다. 모르면 물어본다 (예: 건우 / 유니)."

_P_COLLECT = ("- **로그인된 Chrome + Claude in Chrome 확장** — adsystem 세션 쿠키가 httpOnly 라 "
              "파이썬으로는 못 긁는다. Chrome 에서 fetch 로 받아 로컬 수신 서버에 넘기는 구조다.\n"
              "  확장이 없거나 로그인이 안 돼 있으면 **시작하지 말고** 요청한 뒤 기다린다.")

PREREQ = {
    'inv-daily':     [_P_COLLECT],
    'apple-daily':   [_P_SYNO, _P_CHROME, _P_IMPACT, _P_NAME],
    'disney-daily':  [_P_SYNO, _P_CHROME, _P_OTP, _P_NAME],
    'weekly':        [_P_SYNO, _P_CHROME, _P_IMPACT, _P_NOTION, _P_NAME],
    'dweekly':       [_P_SYNO, _P_CHROME, _P_OTP, _P_NAME],
    'monthly':       [_P_SYNO, _P_CHROME, _P_IMPACT, _P_NOTION, _P_NAME],
    'apple-notion':  [_P_SYNO, _P_NOTION],
    'disney-notion': [_P_SYNO, _P_NOTION],
    'disney-sheet':  [_P_SYNO, _P_GDRIVE],
    'cledger':       [_P_SYNO, _P_NAME],
    'cmonthly':      [_P_SYNO, _P_NAME],
    'nweekly':       [_P_SYNO, _P_NOTION],
    'nsettle':       [_P_SYNO, _P_NAME],
    'kpi-weekly':    [_P_SYNO, _P_NAME],
    'preclose':      [_P_SYNO, _P_IMPACT, _P_NAME],
}


def full_prompt(sid):
    """준비물 점검 + 본 프롬프트. `{from}`·`{to}` 자리는 그대로 남겨 둔다."""
    sk = SKILLS.get(sid)
    if not sk:
        return None
    pre = PREREQ.get(sid)
    head = _PREREQ.format(prereq='\n'.join(pre)) + '\n' if pre else ''
    return head + sk['prompt']
